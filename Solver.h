#include"Sudo.h"

bool SudoSolve(char board[9][9])
{
    for(int i=0;i<9;i++)
    {
        for(int j=0;j<9;j++)
        {
            if(board[i][j] == 'X')
            {
                for(char k=1;k<=9;k++)
                {
                    // insertElement(k);
                }
            }
        }
    }
}